export const selectNotices = (state) => state.notices.noticesData;
export const selectTotalPages = (state) => state.notices.totalPages;
export const selectFavorites = (state) => state.notices.noticesFavorites;
